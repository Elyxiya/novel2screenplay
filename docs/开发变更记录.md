# Novel2Screenplay 开发变更记录

> 整理时间：2026-08-28 | 数据来源：仓库 git 对象库 + 方案/质量文档（`.trae/specs/*`、`.trae/documents/*`）

## 开发总览

| 阶段 | 时间 | 主题 |
|------|------|------|
| Phase 0 | 2026-06-05 ~ 06-07 | 核心功能搭建：Schema、LLM 层、四阶段流水线、API、Web UI |
| Agent 化前置 | 2026-06-28 | PipelineExecutor 接入 PipelineEngine（LLM Adapter / 队列 / SSE / UI 组件） |
| Agent 化 M1/M2 | 2026-08-02 | Agent LLM Adapter + 多 Agent 编排 + 调试日志 + 流程可视化与评测 + Token 优化 |
| P0 短剧闭环 | 2026-08-03 ~ 08-05 | 小说→剧本→短剧分镜全链路 + 创作模块 MVP |
| P1 工程化 | 2026-08-04 ~ 08-07 | 认证覆盖、CI、服务端持久化、monorepo 化、共享包抽取 |
| P1 功能补全 | 2026-08-08 ~ 08-09 | 数据隔离补漏、P-评估、P-记忆、FR-7 中途追问、自定义 LLM、历史收敛 SQLite |
| 架构基座 | 2026-08-27 | 双执行系统收敛、@novel/db 抽取（解 C2/C3）、产品线边界、玻璃拟态 UI、分镜深化、API Key 加密 |
| PG 多后端 | 2026-08-27 ~ 08-28 | 双后端存储抽象（SQLite/PG）+ 迁移脚本 + 参数化契约测试（Task 1-6，R6 待环境） |

## Phase 0：核心功能搭建（2026-06-05 ~ 06-07）

分支：`main` / `dev` + 并行 feature 分支（`feat/schema-definition`、`feat/llm-provider`、`feat/conversion-pipeline`、`feat/api-routes`、`feat/web-ui`、`feat/chapter-select`、`feat/editor-delete`、`feat/yaml-history` 等）

| 日期 | 提交 | 变更 |
|------|------|------|
| 06-05 | `815b18d4` | chore(init)：初始化 Next.js + TypeScript + Tailwind，安装核心依赖 |
| 06-05 | `908b6904` `d8cd9001` | feat(llm)：LLM Provider 抽象层（DeepSeek + OpenAI + TokenBucket 限流） |
| 06-05 | `d151e4ac` | docs(readme)：完善项目 README（中文文档） |
| 06-05 | `1d8972a0` `7055057e` | feat(pipeline)：四阶段 LLM 流水线（含预处理/恢复/取消/提示词） |
| 06-06 | `157fc19e` | feat(schema)：Screenplay Zod Schema（discriminatedUnion / sourceRefs / confidence） |
| 06-06 | `2efa80f2` | feat(pipeline)：四阶段流水线完整实现 |
| 06-06 | `8b777aaf` | feat(api)：8 个 API 路由（upload/start/status/cancel/resume/result/models/cost-estimate） |
| 06-06 | `d61b7975` | feat(ui)：完整 Web UI（上传/配置/转换/结果四页面） |
| 06-07 | stash 记录 | 结果页原文对照分屏、PATCH API 校验增强 + YAML 序列化简化 |

同期迭代要点（详见 `docs/项目介绍完整文档.md`）：safe-json 四层降级解析、防幻觉 Prompt、globalThis 挂载状态防热重载丢失、章节局部坐标修复跨章偏移冲突、GBK 编码检测 + UTF-8 BOM、并发信号量 + 限流。

## Agent 化前置：执行器接入（2026-06-28）

| 日期 | 提交 | 变更 |
|------|------|------|
| 06-28 | `17c37a9e` | refactor：PipelineExecutor 接入 PipelineEngine，Job 系统与四阶段流水线打通，补单元测试 |

`docs/DEVELOPMENT.md`（2026-06-28）记录了同批开发的完整明细：LLM Adapter 统一接口（`adapter/types`、`router`、`deepseek-adapter`、`openai-adapter`）、5 种多模型路由策略、预算控制器、Worker/后台任务系统、Job SSE 进度推送、UI 组件（ModelSelector/ProgressTracker/JobListPanel）。该分支后续以 `cafb9a55` 合入主线。

## Agent 化 M1/M2 + Token 优化（2026-08-02）

分支：`feat/agent-llm-adapter`、`feat/debug-visualization`、`feat/token-optimization`

| 提交 | 变更 |
|------|------|
| `d6d435c2` `11eed4f6` | feat(debug)：流程可视化与效果评测。FlowEvaluator 四阶段确定性评测（format/consistency/coherence/drama 加权评分）、PipelineEngine phaseTimings 插桩、`GET /api/debug/flow-eval`、`/debug` 页面、11 个单测 |
| `cafb9a55` | feat：Agent LLM Adapter + 编排 + 调试日志 + 流程可视化与评测（含上述分支的 LLM Adapter 层、SQLite 持久化、SSE、多 Agent 编排、Worker/Job 等历史内容） |
| `dd7cafd2` `0f310ce9` | perf：Token 优化与解析效率提升。场景级上下文裁剪（按 keyCharacterNames/sourceChapterIndex 过滤）、tiktoken 编码器单例化、convert-scene Prompt 精简约 50%、Phase2 按章过滤角色、FlowEvaluator 新增 Token 效率维度 |

方案文档：`docs/Agent化改造方案.md`、`docs/Agent调试日志方案.md`、`docs/Agent流程可视化与评测方案.md`、`docs/Token优化与解析效率方案.md`。

## P0：短剧闭环与创作模块（2026-08-03 ~ 08-05）

分支：`feat/p0-shortdrama-chain` → `feat/p0-shortdrama-main` → `main`

| 日期 | 提交 | 变更 |
|------|------|------|
| 08-03 | `a93067c9` | feat(p0)：打通小说→剧本→短剧闭环。drama.schema 契约（novel2drama-v1）+ YAML 序列化器、dramatize 转换引擎（景别/运镜/时长/画面提示词）、dramas 存储表（schema_version v3，多用户隔离）、`/api/drama/*` API、`/shortdrama` 分镜页面、22 个新单测 |
| 08-03 | `43c3b0d6` | 同上内容合入 dev（PR #21，含前序功能基线：认证/首页/结果页工具栏/多用户数据隔离） |
| 08-03 | `e347df90` | merge：以 main 为主线合并 dev 的 P0 短剧分镜功能（解决 20 个冲突） |
| 08-03 | `3471c0ac` | fix：useSearchParams 页面包裹 Suspense 边界（Next 16 构建要求） |
| 08-03 | `652a7dcb` | chore：忽略残留 worktree 目录并移除 gitlink 引用 |
| 08-04 | `9110b8df` | fix：生成短剧分镜跳转改用 router.push 软导航，消除整页跳转 ERR_ABORTED 噪音 |
| 08-04 | `e35601cc` | fix：短剧分镜链接关闭 prefetch，消除 Link 预取中止噪音 |
| 08-04 | `32df70ff` `08e041e2` | feat(p1a)：认证覆盖全部业务页 + 401 统一跳转登录回跳 |
| 08-04 | `fa7691ab` | feat(p1b)：短剧分镜增值 - 编辑/删除/统计/导出（PATCH/DELETE `/api/drama/[id]`、repository.update、统计面板、JSON/Excel/YAML 导出） |
| 08-04 | `58560c68` `df008623` | feat(p1c)：CI 流水线 + 构建修复（lint/typecheck/test/build 四 job、auth/register 补 Suspense、build 前清理缓存） |
| 08-04 | `480a92d4` `c441cb71` | feat(p1d)：history 页接入服务端持久化（DELETE `/api/jobs/[id]` SQLite 删除、按 userId 归属校验越权 403、history 从 localStorage 改为 fetch `/api/jobs/history`） |
| 08-04 | `25cc08f9` | feat(p0)：writer 创作模块 MVP 收口（novel.schema 契约、writer-novel-repository、创作台 API、三栏编辑器、AI 续写/扩写/改写/润色、E2E 全流程验证） |
| 08-05 | `fc06fa68` | docs(plan)：P0 链路补齐阶段标记完成，当前阶段切至 P1 |
| 08-05 | `66a8701d` | docs：新增 P0 阶段发布说明（`docs/p0-release-notes`） |

## P1：monorepo 化与共享包抽取（2026-08-05 ~ 08-07）

分支：`feat/p1-monorepo-skeleton` → `feat/p1-s3-auth`

| 日期 | 提交 | 变更 |
|------|------|------|
| 08-05 | `5577e70c` | P1-S2：抽取 `@novel/contracts` Zod schema 包（#25）。所有 zod 契约与序列化器迁入 workspace 包，修复 monorepo 构建顺序、rolldown/lightningcss/oxide 原生绑定，新增 test:coverage 脚本 |
| 08-06 | `9c571756` | P1-S3：抽取 `@novel/auth` 认证包（#26） |
| 08-07 | `66673fc3` | fix(screenplay)：dev 切换到 webpack 模式（`next dev --webpack`，Turbopack 在 Windows 沙箱下 external 复制与绝对路径 import 支持不完整），dev/build 分目录 `.next-dev` / `.next-build`，dev 模式补 externalsPresets.node 修复 `Can't resolve 'path'`，tsconfig paths 去掉 .d.ts 后缀 |
| 08-07 | `abf72d67` | chore(screenplay)：记录 dev 构建目录（.next-dev）的类型 include 配置 |

## P1 功能补全（2026-08-08 ~ 08-09）

P1 主线合入后进入功能补全与闭环阶段：多用户数据隔离、LLM 质量评估、Agent 任务持久化、对话式中途追问、自定义 LLM 接入、历史收敛 SQLite。

| 日期 | 提交 | 变更 |
|------|------|------|
| 08-08 | `071bcba` | test：补充 token 优化验证单测（场景裁剪 + Prompt 长度断言） |
| 08-09 | `50502dc` | feat(p1-3)：补齐 /debug 页面守卫与 API 鉴权（P-安全） |
| 08-09 | `3484f98` | feat(p1-5)：质量关卡人工介入分支（FR-4 后半段） |
| 08-09 | `568fe6d` | feat(p1-4)：会话/任务与 users 表打通 —— 多用户数据隔离补漏 |
| 08-09 | `bca4f58` | feat(P-评估)：传统管线接入 LLM 质量评估 + 质量基准集（PR #35） |
| 08-09 | `ef5eaf8` | feat(screenplay)：持久化 Agent 编排任务（P-记忆，PR #36）——agent_tasks 表 + 重启恢复 |
| 08-09 | `2942f02` | feat(eval)：基准集扩充至 8 样本并据真实评估校准档位 |
| 08-09 | `c0131c2` | feat(history)：历史记录由 localStorage 收敛至 SQLite（`/api/jobs/history`，删除 history-store） |
| 08-09 | `9210070` `4cdfea7` | feat(agent)：FR-7 对话式中途追问（revise 人工介入 + 结果页局部追问 L1/L2） |
| 08-09 | `8e6caaa` | feat(llm)：接入自定义 API（Anthropic / OpenAI 兼容格式，CustomOpenAI/CustomAnthropic Provider） |
| 08-09 | `72e854b` | chore(test)：集中放置测试文件至 `src/__tests__` |
| 08-09 | `8bd45a7` `82d9316` | fix/test(llm)：revise route 补 initializeProviders + DeepSeek 补 supportedModels；registry 按模型 ID 查找与 getDefault 优先级单测 |
| 08-09 | `b1a452c` `9b8f88e` | fix/chore：消除全量 lint unused-vars warning（24 处）+ 清理死代码 |

## 架构基座与视觉升级（2026-08-27）

依据 `docs/产品线归属清单.md` + `.trae/specs/arch-baseline/` 落地架构定型：双执行系统收敛为单一主线、抽取 `@novel/db` 解除 C2/C3 反依赖、产品线边界 lint 冻结；同期完成玻璃拟态 UI、分镜深化、API Key 加密。

| 提交 | 变更 |
|------|------|
| `ee1fc88` | chore(jobs)：删除旧内存队列死代码（job-queue/worker），收敛单一执行主线 |
| `f7bbeb9` | refactor(db)：抽 `@novel/db` 契约化 job 存储（contracts 新增 pipeline 契约），解除 C2 反向依赖 |
| `02cc3ab` | refactor(tools)：builtin-tools 下沉为通用，引入 BuiltinToolDeps 注入解除 C3 |
| `720d369` | chore(arch)：产品线边界 lint 防线（import/no-restricted-paths）+ 归属清单文档 |
| `8deea4b` | chore(scripts)：脚本迁入 scripts/ 分目录 + .trae 规格与 e2e 文档归档 |
| `8aa9726` | feat(ui)：统一玻璃拟态+霓虹视觉，重构顶部导航与移动端适配 |
| `83ae164` | feat(drama)：分镜深化规则增强 + 分镜快照只读契约重构 |
| `7ce4baa` | feat(llm)：API Key AES-GCM 加密存储（api-key-cipher）+ 用户级 LLM 接入 + 连通性测试 |
| `d8cb7e0` | test(e2e)：新增 P0 全链路贯通验收脚本与演示文档 |
| `e41a349` | docs(arch)：AGENTS 项目指南 + 架构基座规格文档 |

## PG 多后端迁移（2026-08-27 ~ 08-28，工作区未提交）

计划：`.trae/specs/pg-multitenant/`；质量报告：`.trae/documents/pg-multitenant-quality-report.md`。Task 1–6 全量完成，双后端契约测试参数化双跑；R6（真实 PG 联跑）待 `DATABASE_URL` 配置。

- **双后端引擎**：`@novel/db` DbEngine 同步抽象（postgres-engine + worker/SharedArrayBuffer/Atomics 桥 + engine-registry + schema-pg + pg-params 占位符翻译），`DATABASE_URL` 存在且 healthCheck 通过启用 PG，否则回退 SQLite。
- **参数化测试**：`dual-backend.ts`（PG 连接解析到 `_test` 后缀库，DB 级隔离）+ `schema-parity.test.ts`（33 例）+ `repository-contract.test.ts`（14 例，9 个 repository 全覆盖）+ `dual-backend.test.ts`（6）+ `engine-registry.test.ts`（5）。
- **迁移脚本**：`packages/db/scripts/migrate-sqlite-to-pg.mjs`（按外键顺序搬 9 表、幂等、双向校验，`--backup/--truncate/--dry-run`，dry-run 实测通过）。
- **回归**：449 例单测全绿 + e2e-p0-fullchain ALL OK + typecheck/lint 全绿。

## 分支结构（截至 2026-08-28）

| 分支 | 状态 | 说明 |
|------|------|------|
| `main` | `e41a349` | 主线（架构基座 + AGENTS 指南），origin/main 同步 |
| `dev` | 历史集成分支 | P0 短剧分镜 #21 时代产物，功能已并入 main |

> 注：PG 多后端相关改动（Task 1–6）目前在工作区未提交，待回归全绿后入库。

## 完整提交明细（对象库可验证，按时间正序）

| 时间（+08:00） | 提交 | 说明 |
|------|------|------|
| 06-05 17:46 | `815b18d4` | chore(init)：初始化脚手架 |
| 06-05 17:51 | `908b6904` | feat(llm)：LLM Provider 抽象层 |
| 06-05 17:53 | `d8cd9001` | feat(llm)：LLM Provider 抽象层 |
| 06-05 18:12 | `d151e4ac` | docs(readme)：完善 README |
| 06-05 18:32 | `1d8972a0` | feat(pipeline)：四阶段流水线 |
| 06-05 21:51 | `7055057e` | feat(pipeline)：四阶段流水线 |
| 06-06 12:01 | `157fc19e` | feat(schema)：Screenplay Schema (#2) |
| 06-06 12:13 | `2efa80f2` | feat(pipeline)：四阶段流水线 |
| 06-06 12:42 | `8b777aaf` | feat(api)：8 个 API 路由 |
| 06-06 12:54 | `d61b7975` | feat(ui)：四页面 Web UI |
| 06-07 17:19 | stash | 原文对照分屏（index/WIP on dev） |
| 06-07 20:22 | stash | PATCH API 校验增强 + YAML 序列化简化 |
| 06-28 15:31 | `17c37a9e` | refactor：PipelineExecutor 接入 PipelineEngine |
| 08-02 16:09 | `d6d435c2` | feat(debug)：流程可视化与效果评测 |
| 08-02 16:22 | `11eed4f6` | feat(debug)：流程可视化与效果评测 |
| 08-02 16:34 | `cafb9a55` | feat：Agent LLM Adapter + 编排 + 调试日志 + 评测 |
| 08-02 18:41 | `dd7cafd2` | perf：Token 优化 |
| 08-02 19:06 | `0f310ce9` | perf：Token 优化与解析效率提升 (#20) |
| 08-03 19:20 | `a93067c9` | feat(p0)：短剧分镜闭环 |
| 08-03 19:45 | `43c3b0d6` | feat(p0)：短剧分镜闭环 (#21) |
| 08-03 20:21 | `e347df90` | merge：main 合并 dev P0 短剧功能（20 冲突） |
| 08-03 20:52 | `3471c0ac` | fix：Suspense 边界（Next 16） |
| 08-03 21:56 | `652a7dcb` | chore：忽略 worktree 目录 |
| 08-04 05:44 | `9110b8df` | fix：router.push 软导航 |
| 08-04 05:47 | `e35601cc` | fix：关闭 Link prefetch |
| 08-04 06:05 | `32df70ff` | feat(p1a)：认证覆盖业务页 |
| 08-04 06:20 | `fa7691ab` | feat(p1b)：短剧分镜编辑/删除/统计/导出 |
| 08-04 12:15 | `08e041e2` | feat：认证 + 短剧分镜增值（合入 main） |
| 08-04 12:18 | `9658371b` | feat：认证 + 短剧分镜增值 |
| 08-04 19:12 | `58560c68` `df008623` | feat(p1c)：CI 流水线 + 构建修复 |
| 08-04 20:28 | `480a92d4` `c441cb71` | feat(p1d)：history 服务端持久化 |
| 08-04 23:47 | `25cc08f9` | feat(p0)：writer 创作模块 MVP |
| 08-05 00:51 | `fc06fa68` | docs(plan)：P0 完成、切入 P1 |
| 08-05 00:59 | `66a8701d` | docs：P0 发布说明 |
| 08-05 19:13 | `5577e70c` | P1-S2：contracts 包 (#25) |
| 08-06 20:38 | `9c571756` | P1-S3：auth 包 (#26) |
| 08-07 23:09 | `66673fc3` | fix：dev 切换 webpack 模式 |
| 08-07 23:20 | `abf72d67` | chore：记录 .next-dev include 配置 |
| 08-08 | `071bcba` | test：token 优化验证单测 |
| 08-09 | `b2cf126` | feat(p0-4)：Agent 对话工作台前端 UI + 修复四阶段空输出 (#29) |
| 08-09 | `50502dc` | feat(p1-3)：补齐 /debug 页面守卫与 API 鉴权 |
| 08-09 | `3484f98` | feat(p1-5)：质量关卡人工介入分支 |
| 08-09 | `568fe6d` | feat(p1-4)：多用户数据隔离补漏 |
| 08-09 | `bca4f58` | feat(P-评估)：LLM 质量评估 + 质量基准集 (#35) |
| 08-09 | `ef5eaf8` | feat(P-记忆)：Agent 编排任务持久化 (#36) |
| 08-09 | `2942f02` | feat(eval)：基准集扩充至 8 样本并校准档位 |
| 08-09 | `c0131c2` | feat(history)：历史记录收敛至 SQLite |
| 08-09 | `9210070` | feat(agent)：FR-7 对话式中途追问（revise 人工介入） |
| 08-09 | `8e6caaa` | feat(llm)：接入自定义 API（Anthropic / OpenAI 兼容） |
| 08-09 | `4cdfea7` | feat(agent)：FR-7 中途关卡挂起 + 结果页局部追问（L1+L2） |
| 08-09 | `72e854b` | chore(test)：测试文件集中至 src/__tests__ |
| 08-09 | `8bd45a7` | fix(llm)：revise route 补 initializeProviders + DeepSeek 补 supportedModels |
| 08-09 | `82d9316` | test(llm)：registry 按模型 ID 查找与 getDefault 优先级 |
| 08-09 | `9b8f88e` | chore：消除全量 lint unused-vars warning（24 处）+ 清理死代码 |
| 08-09 | `b1a452c` | fix：替换 as any 为类型化私有访问，修复 lint |
| 08-27 | `7ce4baa` | feat(llm)：API Key AES-GCM 加密 + 用户级 LLM + 连通性测试 |
| 08-27 | `83ae164` | feat(drama)：分镜深化规则增强 + 分镜快照只读契约重构 |
| 08-27 | `8aa9726` | feat(ui)：玻璃拟态 + 霓虹视觉，重构顶部导航与移动端适配 |
| 08-27 | `720d369` | chore(arch)：产品线边界 lint 防线 + 归属清单文档 |
| 08-27 | `8deea4b` | chore(scripts)：脚本迁入 scripts/ 分目录 + 文档归档 |
| 08-27 | `ee1fc88` | chore(jobs)：删除旧内存队列死代码，收敛单一执行主线 |
| 08-27 | `f7bbeb9` | refactor(db)：抽 @novel/db 契约化 job 存储（解 C2） |
| 08-27 | `02cc3ab` | refactor(tools)：builtin-tools 下沉 + BuiltinToolDeps 注入（解 C3） |
| 08-27 | `d8cb7e0` | test(e2e)：P0 全链路贯通验收脚本与演示文档 |
| 08-27 | `e41a349` | docs(arch)：AGENTS 项目指南 + 架构基座规格文档 |

## 后续建议

- **PG 多后端工作区改动待入库**：Task 1–6 的引擎/测试/迁移脚本改动当前未提交（`git status` 可见），建议先按回归清单（449 例单测 + typecheck + lint + e2e）复验后再提交
- **R6 真实 PG 联跑**：配置 `DATABASE_URL` 后重跑 `npm test`（自动追加 PG 双后端空间）+ 迁移脚本全量模式 + e2e on PG
- `docs/DEVELOPMENT.md` 停留在 2026-06-28 的 Agent 化前置阶段，内容已并入本记录，可归档或删除
- git CLI 需先加 `E:\浏览器下载\Git\cmd` 到 PATH 才能在本环境使用
