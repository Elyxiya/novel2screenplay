{
  "name": "@novel/db",
  "version": "0.1.0",
  "private": true,
  "description": "Novel2Screenplay 共享存取层：job 状态存取抽象 + 契约驱动 codec",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@novel/contracts": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.8",
    "zod": "^4.4.3"
  }
}