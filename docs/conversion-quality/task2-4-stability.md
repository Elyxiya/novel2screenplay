# T2-C4《judge 稳定性报告》基线（合成贴线 + 真实样本）

> 生成时间：2026-08-30T08:48:30.972Z ｜ model=deepseek-chat ｜ reruns=5 双评委（temp 0.2/0.7）
> 成本说明：贴合点 1b 限成本纪律；judge 复跑 80 次调用（含真实样本 zhanlan-short，量级极小，均在预算内）。

## 1. 目的与适用边界

Judge（identity-contradiction）是语义断言的双评委，其复跑方差决定「尾段曲线差值」是否落在噪声带内（T5-C1 R2/R3）。在合成贴线基线之外，本报告追加运行了**真实样本** `zhanlan-short`（其切片含真实断言：揭示/死亡见其 annotation），以真实文本测 judge 复跑噪声带。

- **合成基线**：judge 对干净/矛盾/边界内容的复跑噪声带，供任一真实语义断言样本注入时复用判据（Δ_tail 不因贴线样本而悬空）。
- **真实样本（本报告实测数据）**：`zhanlan-short` 真实切片 × k=5 双评委已实跑，其噪声带见 §2。

## 2. 复跑结果（每格 5×2 分数）

| 格子 | 期望 | mean | stdDev | 噪声带(max2SD,95%CI) | Δ_tail |
|---|---|---|---|---|---|
| judge-real-zhanlan-short | 真实切片（净化叙事：判干净则低方差，若含尚未化解的矛盾则高方差） | 100 | 0 | 0 | 5 |
| judge-baseline-clean | pass（身份一致） | 100 | 0 | 0 | 5 |
| judge-baseline-contradiction | fail（身份分裂/对立） | 23.3 | 35.34 | 70.67 | 71 |
| judge-baseline-boundary | 边界（轻微矛盾，易晃动） | 100 | 0 | 0 | 5 |

> **Δ_tail 全局值（保守取各格最大）**：71 百分点。判据：仅当尾段差 ≥ 该值且总分不劣，才考虑翻默认（flip）。

## 3. 结论（如实，不强行）

- 首行 `judge-real-zhanlan-short` 为真实切片复跑噪声带，其余三格为合成基线。全局 Δ_tail = 71。
- 真实样本切片 `zhanlan-short`（净化叙事）复跑 **100/0 方差**：judge 对真实 clean 写作稳定（判通过、低噪声带）。
- **合成矛盾格复跑不稳定（本轮如实）**：其噪声带在两次参考运行间从 **34.08 → 70.67** 漂移（Δ_tail 对应 35 → 71），说明合成"身份对立"片断对 judge 的方差在不同烧录间亦波动大——**Δ_tail 宜取保守大值（取多轮最大值）**，避免把合成噪声错当信号。
- **flip 决策维持 `false`**（真实语义断言的 judge 方差已由采样切片实测，Δ_tail 按本轮保守值 **71** 供尾段差裁决复用；贴线样本的规则格+占位率差口径不变），与 `flip-decision-record.md` 一致。
