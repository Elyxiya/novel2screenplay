# Novel2Screenplay 项目框架

> 版本：v0.1.0 | 整理时间：2026-08-08 | 当前阶段：P1（共享层抽取与 monorepo 化）

## 项目定位

Novel2Screenplay 是基于 AI 的小说转剧本工具：用大语言模型把叙事小说转换为结构化 YAML 剧本，并进一步拆分为短剧分镜，提供可视化的编辑、评测与导出能力。项目从 2026-06-05 启动，单人开发，当前已从单应用演进为 monorepo。

## 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Next.js 16（App Router，dev/build 均用 webpack） |
| 语言 | TypeScript 5 |
| UI | React 19 + Tailwind CSS 4 |
| 数据库 | SQLite（better-sqlite3，schema_version v3） |
| AI 集成 | 自定义 LLM 抽象层（DeepSeek 主 / OpenAI 备） |
| 序列化 | YAML + Zod 4 校验（tiktoken 费用估算） |
| 测试 | Vitest + Testing Library + MSW |
| 工程 | npm workspaces monorepo + GitHub Actions CI + Docker |

## 仓库结构

```
novel2screenplay/
├── apps/
│   └── screenplay/          # 主应用（Next.js，端口 3001）
├── packages/
│   ├── contracts/           # @novel/contracts：Zod 数据契约（novel/screenplay/drama）
│   └── auth/                # @novel/auth：认证与会话
├── docs/                    # 方案与记录文档
├── short-drama-plan/        # P1 规划演示（HTML）
├── .github/workflows/ci.yml # CI：lint / typecheck / test / build
├── Dockerfile + docker-compose.yml
└── package.json             # workspaces 根
```

P1 规划中的 `@novel/db`、`@novel/agents`、`@novel/ui` 包尚未抽取（见 `packages/README.md`），当前共享包为 `contracts` 与 `auth` 两个。

## 应用结构（apps/screenplay）

### 页面（src/app）

| 页面 | 路由 | 说明 |
|------|------|------|
| 首页 | `/` | 入口 |
| 上传 | `/upload` | 小说上传 / 文本粘贴 |
| 配置 | `/configure` | 章节选择 + 模型选择 + 费用预估 |
| 转换 | `/convert` | 四阶段进度（SSE 实时推送） |
| 结果 | `/result/[id]` | 原文对照 + 可视化编辑 + 导出 |
| 短剧分镜 | `/shortdrama` | 分镜表 + YAML 视图 + 溯源回跳 |
| 创作台 | `/writer`、`/writer/[id]` | 创作模块（章节树/正文/人物卡/世界观/AI 助手） |
| 历史 | `/history` | 服务端持久化任务历史 |
| 调试 | `/debug` | 流水线可视化 + 评测评分卡 + LLM 日志回放 |
| 认证 | `/auth/login`、`/auth/register` | 登录注册 |
| 其他 | `/features`、`/guide`、`/settings`、`/workbench` | 功能说明 / 引导 / 设置 / 工作台 |

### API 路由（src/app/api）

| 分组 | 路由 |
|------|------|
| 流水线 | `/api/pipeline/start`、`status/[jobId]`、`stream/[jobId]`（SSE）、`cancel/[jobId]`、`resume/[jobId]` |
| 任务 | `/api/jobs`、`[id]`、`[id]/events`（SSE）、`history` |
| 剧本 | `/api/result/[jobId]`、`/api/novels`、`novels/[id]`、`/api/import/yaml` |
| 短剧 | `/api/drama`、`drama/[id]`（GET/PATCH/DELETE）、`drama/convert` |
| 创作 | `/api/writer/novels`、`writer/novels/[id]`、`chapter`、`convert`、`ai` |
| Agent | `/api/agent/start`、`agent/stream/[taskId]` |
| 调试 | `/api/debug/flow-eval`、`debug/agent-logs` |
| 认证 | `/api/auth/login`、`logout`、`me`、`register`、`password` |
| 其他 | `/api/health`、`/api/models`、`/api/cost-estimate`、`/api/upload` |

### 核心模块（src/lib）

```
lib/
├── pipeline/    # 四阶段转换流水线：ContextManager + Phase1Analyzer +
│                #   Phase2Segmenter + Phase3SceneConverter + Phase4Merger +
│                #   PipelineEngine + executor
├── llm/         # Provider 抽象：BaseProvider / DeepSeek / OpenAI + registry +
│                #   rate-limiter + prompts（analyze/segment/convert-scene）
│   └── adapter/ # 模型路由：types / router / routing-strategy /
│                #   budget-controller / deepseek-adapter / openai-adapter
├── novel/       # 小说解析（7 种章节正则、GBK 编码检测、章节局部坐标）
├── drama/       # 短剧分镜：dramatize 转换引擎 + drama-stats + drama-export
├── agent/       # Agent 运行时：AgentCore（状态机+记忆+工具循环）
│   ├── llm/     #   AgentLLMProvider / PromptEngine / tool-registry / message-converter
│   └── debug/   #   conversation-logger / logging-llm-provider / logging-tool-executor
├── multi-agent/ # 编排：roles / registry / handoff-protocol / handoff-manager /
│                #   review-gate / orchestrator / agent-config
├── tools/       # 内置工具与注册表（builtin-tools / tool-registry）
├── jobs/        # 后台任务：job-queue / worker / types
├── sse/         # Server-Sent Events：sse-client-manager（多客户端、心跳、重连）
├── store/       # 存储：job-store / history-store + sqlite/（db、schema.sql、
│                #   job/novel/drama/history/project/user/writer-novel repository）
├── debug/       # flow-evaluator（四维评分 + Token 效率维度）
└── utils/       # logger / safe-json（四层降级解析）
```

### 关键组件（src/components）

`AuthForm`、`AuthStatus`、`RequireAuth`（业务页认证守卫 + 401 跳登录回跳）、`HeaderNav`、`ModelSelector`、`ProgressTracker`（SSE + 轮询降级）、`WorkflowStepper`、`HistoryPanel`、`JobListPanel`、`compare/SceneCompare`（原文对照）、`editors/SceneEditor`、`CharacterEditor`、`LocationEditor`（可视化编辑）、`debug/FlowDebugClient`、`ui/EmptyState`、`ErrorBoundary`、`LoadingSkeleton`。

## 数据契约（@novel/contracts）

| 契约 | 内容 |
|------|------|
| `novel.schema` | 创作小说：分卷 / 章节 / 人物卡 / 世界观（Zod 校验） |
| `screenplay.schema` | 剧本 `novel2screenplay-v1`：场景 / 角色 / 地点，sourceRefs 溯源，discriminatedUnion 内容块 |
| `drama.schema` | 分镜 `novel2drama-v1`：景别 / 运镜 / 时长 / 画面提示词 |
| 序列化器 | YAML serializer + drama serializer |

## 核心业务流

1. 小说 → 剧本：上传解析 → 四阶段流水线（分析角色地点 → 场景切割 → 并行场景转换 → 合并去重校验）→ 剧本 YAML → 可视化编辑 → 导出（YAML/JSON/Excel）
2. 剧本 → 短剧分镜：`dramatize` 引擎按规则拆分为分镜，支持编辑 / 删除 / 统计面板 / 导出
3. 创作模块：创作台草稿 CRUD → AI 续写 / 扩写 / 改写 / 润色 → 物化转剧本

## 关键机制

- **流水线健壮性**：信号量 3 并发 + Token Bucket 限流（50 req/min）+ 3 次重试 + 降级输出（`degradedScene`）、任务恢复（从 Phase 3 重跑失败场景）
- **上下文裁剪（Token 优化）**：Phase3 按 `keyCharacterNames` / `sourceChapterIndex` 过滤角色地点上下文，tiktoken 编码器单例化，convert-scene Prompt 精简约 50%
- **LLM 输出处理**：safe-json 四层降级解析 + 截断检测 + 防幻觉 Prompt（只写可见可听动作、对白必须源于原文）
- **存储**：SQLite WAL，job / novel / drama / history / project / user / writer-novel 多表，按 userId 数据隔离（越权 403）
- **实时推送**：SSE（init/progress/phase/log/complete/error/heartbeat 事件）+ 心跳 + 自动重连 + 轮询降级
- **Agent 化**：AgentCore 状态机 + 三级记忆 + 工具调用循环；多 Agent 编排（supervisor/writer/editor/analyzer/validator）+ 交接协议 + 质量关卡；调试日志全量记录 LLM 对话与工具调用
- **效果评测**：FlowEvaluator 纯函数评测（format/consistency/coherence/drama 加权 + Token 效率），`/debug` 页面可视化
- **认证**：会话 Cookie 认证，`@novel/auth` 包（password / session / store），RequireAuth 覆盖业务页

## 工程化

| 项 | 说明 |
|----|------|
| 脚本 | `dev`（next dev --webpack，端口 3001）、`dev:turbo`、`build`、`start`、`lint`、`typecheck`、`test`、`test:coverage` |
| 构建 | prebuild 先构建 contracts/auth 两包；dev/build 分目录 `.next-dev` / `.next-build` |
| CI | lint / typecheck / test（含覆盖率上传）/ build 四个 job，构建前清理缓存并 rebuild better-sqlite3 |
| 部署 | Dockerfile + docker-compose（SQLite 数据卷 `/app/data`，healthcheck 走 `/api/health`） |
| 环境变量 | `DEEPSEEK_API_KEY`（必需）、`OPENAI_API_KEY`、`DB_DIR`、`DB_FILE`、`DEFAULT_MODEL_ID` |

## 已知限制与演进方向

- **双套执行系统并存**：UI 主链路走 `PipelineEngine`，另有 `src/lib/jobs/` 队列仅被 `/api/jobs` 使用，两者进度与日志格式不一致，Agent 化改造方案中计划收敛为一条主线
- **SQLite 单机**：多实例不共享数据，计划迁移 PostgreSQL/Redis
- **共享包未抽完**：P1 规划的 `@novel/db`、`@novel/agents`、`@novel/ui` 尚未落地
- **README 结构说明滞后**：README 的"项目结构"章节仍描述单应用时代的 `src/` 布局，与实际 monorepo 结构不一致，建议同步更新
