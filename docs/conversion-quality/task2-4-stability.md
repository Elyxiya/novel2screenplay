# T2-C4《judge 稳定性报告》基线（合成贴线内容）

> 生成时间：2026-08-30T07:24:43.024Z ｜ model=deepseek-chat ｜ reruns=5 双评委（temp 0.2/0.7）
> 成本说明：贴合点 1b 限成本纪律；judge 复跑 60 次调用（合成内容，量级极小，均在预算内）。

## 1. 目的与适用边界

Judge（identity-contradiction）是语义断言的双评委，其复跑方差决定「尾段曲线差值」是否落在噪声带内（T5-C1 R2/R3）。本报告用**合成贴线语义内容**量化 judge 噪声带基线。

- **合成基线（本报告实测）**：judge 对干净/矛盾/边界内容的复跑噪声带，供后续任一真实语义断言样本注入时复用判据（Δ_tail 不因贴线样本而悬空）。
- **真实样本状态（如实）**：当前标注样本语义断言为零（预注册纪律：不硬造），贴线判据仍走「规则格确定性差异 + 占位率差」退化口径；Δ_tail 实值见下，**但不据此裁决贴线样本**（其尾段差无真实语义断言可对照）。

## 2. 复跑结果（每格 5×2 分数）

| 格子 | 期望 | mean | stdDev | 噪声带(max2SD,95%CI) | Δ_tail |
|---|---|---|---|---|---|
| judge-baseline-clean | pass（身份一致） | 100 | 0 | 0 | 5 |
| judge-baseline-contradiction | fail（身份分裂/对立） | 13.2 | 17.04 | 34.08 | 35 |
| judge-baseline-boundary | 边界（轻微矛盾，易晃动） | 100 | 0 | 0 | 5 |

> **Δ_tail 全局值（保守取各格最大）**：35 百分点。判据：仅当尾段差 ≥ 该值且总分不劣，才考虑翻默认（flip）。

## 3. 结论（如实，不强行）

- 三格 judge 复跑噪声带见上表；全局 Δ_tail = 35。

> **边界格如实告警**：`judge-baseline-boundary` 的合成内容（掌柜"似有别心"冷笑）**未复现预期晃动**——judge 两次都给出 pass（mean=100，方差 0）。说明该合成片段对 judge 而言落在"不构成冲突"的稳定区，未真正命中"轻微矛盾摇摆"档。因此方差基线实际仅由矛盾格驱动：噪声带 34 DB≈judge 对矛盾内容的复跑不稳定性（mean 13.2、sd 17），干净/边界档表现为低噪声。**真正的边界摇摆档需要更贴近线粒度的合成片段才可能测出，属本基线明确边界，勿外推。**
- 当前真实样本**未注入人工语义断言** → 贴线判据仍退化；Δ_tail 基线保留供后续真实语义断言样本触发真实 judge 时复用。
- **flip 决策维持 `false`**（R1 贴线样本缺真实语义 judge 方差 / R3 数据门槛未到），与 `flip-decision-record.md` 一致。
